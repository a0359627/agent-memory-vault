# SECURITY

這份文件有兩個對象：**要把這套東西用在自己知識庫上的人**，以及**要回報本 repo 疑似洩漏的人**。

---

## 一、三條鐵律

這三條的優先權**高於「把事情做完」**。agent 在執行任何任務時都適用，沒有例外、沒有「這次情況特殊」。

### 1. 值永遠不寫進知識庫，也不出現在回應文字裡

任何 API key、token、密碼、私鑰的**值**，都不寫入版本庫的任何檔案，也不寫進 agent 給你的回答。需要提到某把金鑰時，只寫三件事：**它叫什麼名字、做什麼用、放在哪個路徑**。

值與位置是兩種東西。「`SOMETHING_API_KEY` 放在該專案自己 gitignored 的 `.env`，runtime 才讀」是位置；把那串字貼進 Markdown 是值。前者可以寫，後者永遠不行。

### 2. 彙編原始來源時，不把原文裡的金鑰複製進摘要

原始來源（聊天紀錄、交接文件、別人給的設定檔）常常夾帶真的憑證。把它整理成彙編頁時，**那幾行不要跟著過去**——即使你打算「等一下再刪」。摘要頁一旦寫出來就會被索引、被複製、被貼進下一個 context。

原文本身如果必須保存，留在不可變的原文層並確保該路徑已被 gitignore；摘要頁只寫「原文第 N 段含一組憑證，值未複製」。

### 3. 看到落單的金鑰，標記位置回報，不要「順手清理」

發現某個檔案裡有疑似落單的 key 或密碼時，正確動作是**停下來、記下檔名與行號、回報**。

不要自己把它搬到別的檔案、不要「幫忙」改寫成別的格式、不要刪掉就當沒發生過。搬動只會讓它多出現在一個地方（而且通常是在 git 歷史裡多一份）；刪除則讓你失去「這把金鑰已經外流過、必須輪替」的證據。**金鑰一旦寫進過版本庫，唯一有效的補救是輪替它，不是把它從歷史裡挖掉。**

---

## 二、只記位置，不記值

知識庫對 secrets 唯一的職責是回答「**去哪裡找**」。實務上維護一張這樣的表就夠了——本 repo 的 `template/06-reference/secrets-hygiene.md` 提供空白表頭，欄位是：

| 欄位 | 寫什麼 | 不寫什麼 |
|---|---|---|
| 類型 | 這是哪一類憑證（服務 API key／OAuth token／資料庫密碼／部署用私鑰） | 服務帳號的完整識別字 |
| 位置 | 相對描述即可：「該專案自己 gitignored 的環境檔」「本機使用者目錄下、版本庫之外的一份筆記」 | 完整絕對路徑、主機名稱、雲端專案識別字 |
| Git 狀態 | 已 gitignore／從未進過版本庫／曾經進過（已輪替） | — |
| 用途 | 一句話：誰在什麼時候會用到 | 值、值的前幾碼、值的長度 |

三個附帶規則：

- **本機明文清冊（如果你需要一份）留在版本庫之外，並且值以 disk-to-disk 的方式產生**——不經過任何模型的 context。它存在的理由是給人查，不是給 agent 讀。
- **要隨版本庫走的秘密一律先加密**（本 repo 的 template 用 age：密文與公鑰進庫，私鑰永遠不進）。密文與明文分開存放，私鑰只在本機。
- **提交之前確認忽略狀態**：`git check-ignore -v <path>` 比「我記得有加過」可靠。`git status` 沒列出來不等於被忽略，也可能只是還沒建立。

---

## 三、這個 repo 自己的保證與限制

本 repo 由一個私有知識庫**以 allowlist 白名單匯出**而來：只有明確列進清單的檔案會出現在這裡，多一個檔、少一個檔都會讓匯出工具失敗。匯出後還要通過 `bin/gate.sh` 的三層掃描（見 [docs/gates.md](docs/gates.md)）。

因此這裡**應該**沒有任何：真實人名或聯絡方式、公司或客戶識別、雲端專案與主機識別、IP、本機絕對路徑、私有版本庫名稱、憑證的值或位置、真實的專案頁與驗收紀錄。範例資料**全部是虛構的**。

**但掃描器不保證抓到所有機敏資料。** 已知抓不到的類型：

- 別稱、暱稱、縮寫、拼音、音近字；
- 單看每一句都無害、湊起來就能反推的敘述（時間 ＋ 規模 ＋ 領域 ＋ 事故細節）；
- 改寫過的專有名詞（「某某系統」如果只有一家這樣做，等於沒改）。

這些只有人讀得出來，所以人工全文讀是最後一道、也是唯一真正有效的閘門。

## 四、回報疑似洩漏

如果你在本 repo 裡看到疑似真實的識別字、憑證、或可反推特定個人與組織的敘述：

1. **請用 GitHub 的私下回報管道**（repo 的 Security 分頁 → Report a vulnerability）。
2. **不要開公開 issue、不要在 PR 或討論串裡貼上你看到的那串內容。** 只要寫**檔名與行號**，加上一句你為什麼覺得它是真的，就足以定位。
3. 如果你認為那是一把可用的憑證，請直接說「疑似可用憑證」——這類回報會被優先處理成「先輪替、再談要不要改文件」。

反過來，如果你發現的是**你自己的**憑證不小心出現在任何地方（本 repo、你自己的 vault、任何一份 PR）：先輪替，再清理。順序不能顛倒。

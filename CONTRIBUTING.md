# 貢獻指南

歡迎 issue 與 PR。這個 repo 有一個和多數專案不同的前提：**它的內容來自一個私有知識庫**，所以「內容怎麼進來」跟「程式碼怎麼改」是兩套規則。先讀完對應那節再動手。

---

## 一、內容從哪裡來：allowlist 是唯一入口

本 repo 的 `template/`、`skills/`、`docs/REFERENCES.md` 等內容檔，**不是手寫進來的**，而是由私有側的一份白名單 manifest 經 `tools/export_public.py` 匯出：

- manifest 逐檔列出目的路徑、模式（照搬／規則改寫／手寫定版／工具生成）與輸出的 sha256；
- 改寫模式的每一條規則都帶 `expect` 命中次數，**命中 0 次或多於預期都會失敗**——這讓「該改沒改」變成可執行的斷言，而不是檢查清單上的一個勾；
- 匯出工具會比對整棵樹：**repo 裡有但 manifest 沒列的檔案**，以及**manifest 列了但 repo 沒有的檔案**，兩種都是錯誤；
- 任何一個檔案出錯，**一個檔都不寫**。

實務上的意思是：manifest 的四種模式**不是**「全部都不准手改」，而是分成兩類，差別在**真相在哪一邊**。

> **`copy`／`rewrite`——真相在私有母本，不要手改。** 改了會被 `bin/gate.sh --full` 抓出來，而且下一次匯出會把你的修改蓋掉。`template/`、`skills/`、`examples/`、`office-agent-starter-kit/` 底下的內容頁大多屬於這一類。
>
> **`pinned`／`generate`——真相就在本 repo，歡迎送 PR。** `tools/`、`bin/`、`tests/`、`.github/` 與 `docs/` 底下非生成的頁面都是這一類；manifest 只記下它們的 sha256，用來擋「有人改了卻沒人知道」，不代表它們是從別處匯出的。

模式是**逐檔**決定的，只有維護者手上的 manifest 知道完整對照。拿不準就開 issue 問，或直接送 PR 並在描述裡說你不確定——維護者會告訴你該走哪條路。

如果你想改的是第一類檔案的**內容**，請開 issue 說明你認為哪裡該改、為什麼。維護者會在私有側改母本再重新匯出。（`docs/REFERENCES.md` 也走這條路：它由 `tools/build_references.py` 從私有母本生成，第一行就寫著 do not edit。）

**維護者請注意：** 合併第二類的 PR 之後，manifest 裡那幾筆 `pinned` 的 sha256 就過期了，下一次 `bin/gate.sh --full` 一定會紅——這不是誤報，是它該有的行為。合併後要跑：

```bash
python3 tools/export_public.py --manifest <private>/manifest.json --out . --print-hashes
```

把新的 sha256 回填進私有 manifest，再跑一次 `bin/gate.sh --full`。這一步不寫下來，遲早有人用旗標把整道閘門繞過去。

## 二、去識別守則

只要你寫的任何一個字可能來自真實工作，這節就適用——包含 issue 內文、PR 描述、範例、測試 fixture 與 commit 訊息。

**絕對不要出現：** 真實人名、email、電話；公司、客戶、品牌、內部系統名稱；網域；雲端專案識別字與編號；IP 與主機名；本機絕對路徑（三大平台的使用者家目錄開頭那種都算，寫成 `~/` 相對寫法）；私有版本庫名稱；任何憑證的值、位置或前幾碼。

**改寫的標準不是「看不出來」，是「反推不出來」：**

- **不要只換名字。** 把「某某系統」換成「系統 A」而保留它的拓樸、規模與事故時間，等於沒改——知道那個領域的人一眼就認得。
- **拆掉可組合的線索。** 一段敘述如果同時帶著時間、規模、產業與事故細節，四個裡拿掉兩個，通常就從「可定位」變成「只是個通則」。
- **數字最會洩漏。** 具體的配額數、信件數、金額、人數請改成量級或直接刪掉。故事的教訓不需要精確數字才成立。
- **舉例一律虛構。** 需要例子時就編一個。本 repo 的 `examples/sample-vault/` 與 office kit 的 `fixtures/` 全部是虛構的，照著那個尺度寫。
- **判準可以留，脈絡要丟。** 「不叫乾跑，除非它真的零副作用」是通則，值得寫；哪個系統在哪一天因為它出過什麼事，不要寫。

**測試資料**：用 `example.com`／`example.org` 當網域，用 `127.0.0.1` 當位址，用 `<your-token>`、`your-api-key`、`changeme` 這類明顯的佔位符當假值——掃描器對這些有白名單，寫成別的樣子反而會誤報。

## 三、對抗式複核

任何**新增或大幅改寫內容**的 PR（不只是改程式），合併前會經過一次對抗式複核：由另一個 session 的 agent 或另一個人，以「陌生讀者＋找碴」的角度逐檔讀過，專門找兩件事：

1. 疑似殘留的識別字；
2. **可組合推論的敘述**——單看每句都無害，湊起來能定位到特定個人、組織或事件的段落。

複核者只拿到**識別字的類型清單**（人名、公司、客戶、網域、雲端、私有 repo…），**不拿到值**。理由很直接：拿到值的複核者會變成只做字串比對，而字串比對機器已經做完了；人的價值在於抓機器抓不到的那一類。

**通過標準是 0。** 有任何一項就退回改。如果你是 PR 作者，先自己用這個角度讀一遍會省下一輪來回。

## 四、CI 與本機閘門

**PR 必須通過 CI。** 工作流程是 `.github/workflows/gate.yml`，跑的就是 `bin/gate.sh --public`——和你本機跑的是同一支腳本。

送 PR 之前先在本機跑：

```bash
bin/gate.sh --public
```

沒有 HMAC key 時（大多數外部貢獻者都沒有）加上 `--no-blocklist` 跳過 L2 那層：

```bash
bin/gate.sh --public --no-blocklist
```

也建議裝上 pre-commit hook，讓每次 commit 自動掃 staged 檔案：

```bash
tools/install-hooks.sh
```

### fork 的 PR 與 L2

L2（識別字 HMAC 封鎖）需要一把 key，那把 key 存在 repo 的 Actions secret 裡。**GitHub 不會把 secret 給 fork 送來的 PR**，所以：

- **fork 的 PR**：CI 會印一則 notice 並以 `--no-blocklist` 跑，**L1（secret 樣式）、L3（結構與形狀）與全部測試照跑**，不會因為缺 key 就整個變紅。這是刻意的——不然所有外部貢獻都無法通過 CI。
- **same-repo 的分支與 push 到 main**：拿得到 key，跑完整三層。
- **維護者合併 fork PR 之前**，要在 same-repo 分支或本機補跑一次帶 L2 的完整 gate。這是規則，不是建議。

也就是說，對 fork PR 而言，**L2 這層防線是由維護者在合併前補上的**，不是在你的 PR 上跑的。這正是第二節那些去識別守則要靠你自己遵守的原因。

## 五、程式碼慣例

- **Python 3.11 標準庫與 bash，零第三方相依。** 這是硬性規則，不是偏好：`bin/` 會被整包複製進讀者的知識庫，那裡不會有 virtualenv。要用到第三方套件的功能，先開 issue 討論有沒有標準庫的替代路徑。
- **工具預設唯讀。** 任何會寫檔的工具，預設模式是預覽，`--apply` 才真的寫。**「乾跑」必須是零副作用**——會寫入外部系統的東西不叫乾跑，叫「少做一步的正式執行」；如果它會寫，就給它一個誠實的名字。
- **絕不覆蓋使用者既有的東西。** 安裝器遇到同名目錄一律跳過並警告。
- **錯誤訊息只給檔名與行號，永不印出命中的值。** 掃描器的輸出會被貼進 issue、CI log 與聊天視窗；印出值等於把洩漏擴大一輪。
- **新行為要有測試。** 測試一律在 `tempfile` 建立的臨時目錄裡跑，**不碰真實的 HOME、不碰真實的知識庫**。
- **不要在 repo 裡建立符號連結**，gate 會擋（`skill_sync.py` 在讀者自己的 home 建連結是另一回事）。

## 六、送出之前

- [ ] `bin/gate.sh --public`（或 `--no-blocklist`）在本機是綠的
- [ ] 沒有手改由 manifest 管理的內容檔
- [ ] 新增的每個例子都是虛構的，而且反推不出真實對象
- [ ] 沒有新的第三方相依
- [ ] PR 描述說明「改了什麼、為什麼、怎麼驗證的」；測試通過只證明測試通過，不要寫成「已驗收」

發現疑似機敏資料殘留時**不要開公開 issue**，請照 [SECURITY.md](SECURITY.md) 的私下管道回報。
